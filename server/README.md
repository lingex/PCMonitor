# PCMonitor-Server
A simple WebSocket server that collects and pushes basic PC status information to clients.  

Author: Many AIs
Icon: Generate by Gemini

## .exe build environment
- python
- pip
- git bash (for example)

```bash
pip install -r requirements.txt

build build.sh

```
